export default function AddNote({  classAddNote }) {
  return (
    <section>
      <img
        className={classAddNote}
        src={require("../Resourses/nueva-nota.png")}
        alt="Logo Githup"
      />
    </section>
  );
}
